#include <vector>
#include <iostream>
#include "templates.h"

int main()
{
	int sum = 0;
	int prod = 1;
	std::vector<int> vec{ 5, 10, 15 };
	//std::cout << tasks::SumPowerOfFive(vec.begin(), vec.end(), sum) << std::endl;
	//std::cout << tasks::SumFibonacciNumbers(vec.begin(), vec.end(), sum) << std::endl;
	//std::cout << tasks::SumSquareNumbers(vec.begin(), vec.end(), sum) << std::endl;
	//std::cout << tasks::SumPowerOfThree(vec.begin(), vec.end(), sum) << std::endl;
	//std::cout << tasks::NumberDivisible(vec.begin(), vec.end(), sum, 5) << std::endl; // 2 6 7 8
}
